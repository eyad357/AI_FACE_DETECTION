"""
PLACEHOLDER — owned by Person 2 (Robotics / Robot Control).

Future responsibility: drive the physical robot (speech, gestures,
movement) in response to commands defined in robot_commands.py, and
expose an API that the Decision layer can call.

Architectural rule: app.robot must NEVER import app.vision. The Robot
Controller receives application-level commands/events from the Decision
layer, not raw Vision output.

This file is intentionally left unimplemented in Phase 1.
"""
