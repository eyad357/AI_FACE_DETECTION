"""
PLACEHOLDER — not implemented in this phase.

Future responsibility: load a trained artifact from app.ml.artifacts
and run inference (question -> intent) at request time. Consumed by
app.ml.service, which exposes the public IntentClassifier API.
"""
