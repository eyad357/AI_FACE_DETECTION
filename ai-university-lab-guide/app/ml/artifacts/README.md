# ML Artifacts

Reserved for future trained model artifacts (e.g. a serialized
TF-IDF vectorizer + Logistic Regression classifier for intent
classification).

No artifacts exist yet — this phase only prepares the directory.

Trained artifacts should be added to `.gitignore` if they are large or
environment-specific; only small, reviewable artifacts should be
committed directly.
