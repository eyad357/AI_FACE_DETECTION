"""
PLACEHOLDER — not implemented in this phase.

Future responsibility: run gesture-recognition inference on a camera
frame using app.dl.models, producing a GestureResult. Consumed by
app.dl.service, which exposes the public API.
"""
